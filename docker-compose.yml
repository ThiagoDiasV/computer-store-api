version: '3'

services:
  web:
    build: .
    command: bash -c "python silvertec/manage.py migrate && python silvertec/manage.py runserver 0.0.0.0:8000"
    container_name: silvertec
    volumes:
      - .:/silvertec
    ports:
      - "8000:8000"